clustered-canvas-renderer
=========================

A clustered canvas renderer for openlayers 2

## Usage
``` 
var layer = new OpenLayers.Layer.Vector('LayerName', {
	renderers: ['ClusteredCanvas']
});
```